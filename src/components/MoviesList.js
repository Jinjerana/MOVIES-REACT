import { useEffect } from 'react';

export const Movies = () => {
  const products = getProducts();
  useEffect;
  get - запрос;
  state;
  State - props;
  return (
    <main>
      <MoviesList movies={movies} />
    </main>
  );
};
